# Demo certificates

This directory is intentionally empty in git.

Generate test certificates on your machine with:

```sh
./mtls-demo/scripts/gen-certs.sh
```

This will create CA, server, and client certs/keys in this folder for local
demo use. Do not commit generated key material.
